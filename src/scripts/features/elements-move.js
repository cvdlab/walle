"use strict";

var ElementsMove = function (walle) {
  this.walle = walle;
  this.paper = walle.paper;
};


/**
 * start
 */
ElementsMove.prototype.start = function () {

  //let walle = this.walle;
  //let scene = walle.scene;
  //
  //walle.addElementsFeedback(['hole']);
  //
  //
  //let startHandler = (event) => console.log('start', event);
  //let stopHandler = (event) => console.log('stop', event);
  //let moveHandler = (event) => console.log('stop', event);
  //
  //scene.onDrag(startHandler, stopHandler, moveHandler);

};

/**
 * stop
 */
ElementsMove.prototype.stop = function () {

};
